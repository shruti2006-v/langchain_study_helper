export function searchNotes(query: string): string {
  const notes = `
LangChain helps developers build LLM applications.
It supports chains, agents, tools, memory, and retrieval.
LangSmith helps trace and debug AI applications.
`;

  return notes;
}